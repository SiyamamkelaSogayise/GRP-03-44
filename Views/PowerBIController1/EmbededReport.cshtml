@* @model EmbedConfig

<div>
    <!-- Embed the Power BI report using JavaScript -->
    <div id="embed-container"></div>

    <script src="https://microsoft.github.io/PowerBI-JavaScript/demo/node_modules/powerbi-client/dist/powerbi.js"></script>

    <script>
        var embedConfig = @Html.Raw(Json.Serialize(Model));

        // Embed the Power BI report
        var reportContainer = $('#embed-container')[0];
        var report = powerbi.embed(reportContainer, embedConfig);

        // Handle any additional configurations or events as needed
    </script>
</div>
 *@